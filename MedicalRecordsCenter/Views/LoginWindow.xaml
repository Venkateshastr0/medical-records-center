<Window x:Class="MedicalRecordsCenter.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="MRC Login - Medical Records Center"
        Height="600"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Border Background="White" 
            CornerRadius="10" 
            BorderBrush="#E0E0E0" 
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" 
                    Background="{StaticResource PrimaryBrush}" 
                    CornerRadius="10,10,0,0"
                    Height="80">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Logo and Title -->
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal" 
                                VerticalAlignment="Center"
                                Margin="20,0">
                        <materialDesign:PackIcon Kind="HospitalBox" 
                                                Foreground="White"
                                                Width="32" 
                                                Height="32"
                                                VerticalAlignment="Center"
                                                Margin="0,0,15,0" />
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="MRC" 
                                       FontFamily="{StaticResource UbuntuMediumFont}"
                                       FontSize="24"
                                       FontWeight="Medium"
                                       Foreground="White" />
                            <TextBlock Text="Medical Records Center" 
                                       FontFamily="{StaticResource UbuntuFont}"
                                       FontSize="12"
                                       Foreground="#E0E0E0" />
                        </StackPanel>
                    </StackPanel>

                    <!-- Close Button -->
                    <Button Grid.Column="1"
                            Style="{StaticResource MaterialDesignIconButton}"
                            Command="{Binding CancelCommand}"
                            Background="Transparent"
                            Foreground="White"
                            Margin="0,0,10,0">
                        <materialDesign:PackIcon Kind="Close" />
                    </Button>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="40,30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Welcome Message -->
                <TextBlock Grid.Row="0"
                           Text="Welcome Back"
                           FontFamily="{StaticResource UbuntuMediumFont}"
                           FontSize="24"
                           FontWeight="Medium"
                           Foreground="#0d171c"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10" />

                <TextBlock Grid.Row="1"
                           Text="Please login to access the Medical Records Center"
                           FontFamily="{StaticResource UbuntuFont}"
                           FontSize="14"
                           Foreground="#666"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30" />

                <!-- Username Field -->
                <TextBox Grid.Row="2"
                         materialDesign:HintAssist.Hint="Username"
                         materialDesign:HintAssist.IsFloating="True"
                         materialDesign:TextFieldAssist.LeadingIcon="Account"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         FontFamily="{StaticResource UbuntuFont}"
                         FontSize="14"
                         Margin="0,0,0,20"
                         MaxLength="50" />

                <!-- Password Field -->
                <PasswordBox Grid.Row="3"
                             materialDesign:HintAssist.Hint="Password"
                             materialDesign:HintAssist.IsFloating="True"
                             materialDesign:TextFieldAssist.LeadingIcon="Lock"
                             x:Name="PasswordBox"
                             FontFamily="{StaticResource UbuntuFont}"
                             FontSize="14"
                             Margin="0,0,0,20"
                             MaxLength="100" />

                <!-- Remember Me & Forgot Password -->
                <Grid Grid.Row="4" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Remember Me -->
                    <CheckBox Grid.Column="0"
                              Content="Remember me"
                              IsChecked="{Binding RememberMe}"
                              FontFamily="{StaticResource UbuntuFont}"
                              FontSize="12"
                              Foreground="#666" />

                    <!-- Forgot Password -->
                    <Button Grid.Column="1"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Content="Forgot Password?"
                            Command="{Binding ForgotPasswordCommand}"
                            FontFamily="{StaticResource UbuntuFont}"
                            FontSize="12"
                            Padding="0"
                            Height="Auto" />
                </Grid>

                <!-- Error Message -->
                <TextBlock Grid.Row="5"
                           Text="{Binding ErrorMessage}"
                           FontFamily="{StaticResource UbuntuFont}"
                           FontSize="12"
                           Foreground="{StaticResource ErrorBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <!-- Login Button -->
                <Button Grid.Row="6"
                        Command="{Binding LoginAsyncCommand}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="{StaticResource PrimaryBrush}"
                        Foreground="White"
                        FontFamily="{StaticResource UbuntuMediumFont}"
                        FontSize="14"
                        FontWeight="Medium"
                        Height="45"
                        Margin="0,20,0,0"
                        VerticalAlignment="Top">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Loading Indicator -->
                        <ProgressBar Grid.Column="0"
                                     Style="{StaticResource MaterialDesignCircularProgressBar}"
                                     IsIndeterminate="True"
                                     Width="20"
                                     Height="20"
                                     Margin="0,0,10,0"
                                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

                        <!-- Button Text -->
                        <TextBlock Grid.Column="1"
                                   Text="{Binding IsLoading, Converter={StaticResource LoginButtonTextConverter}}"
                                   VerticalAlignment="Center" />
                    </Grid>
                </Button>

                <!-- Status Message -->
                <TextBlock Grid.Row="6"
                           Text="{Binding StatusMessage}"
                           FontFamily="{StaticResource UbuntuFont}"
                           FontSize="12"
                           Foreground="#666"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Bottom"
                           Margin="0,0,0,20" />
            </Grid>

            <!-- Footer -->
            <Border Grid.Row="2" 
                    Background="#F5F5F5" 
                    CornerRadius="0,0,10,10"
                    Height="60">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Security Info -->
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal" 
                                VerticalAlignment="Center"
                                Margin="20,0">
                        <materialDesign:PackIcon Kind="Security" 
                                                Foreground="#666"
                                                Width="16" 
                                                Height="16"
                                                VerticalAlignment="Center"
                                                Margin="0,0,5,0" />
                        <TextBlock Text="Secure Login - HIPAA Compliant" 
                                   FontFamily="{StaticResource UbuntuFont}"
                                   FontSize="12"
                                   Foreground="#666"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- Version Info -->
                    <TextBlock Grid.Column="1"
                               Text="v1.0.0"
                               FontFamily="{StaticResource UbuntuFont}"
                               FontSize="10"
                               Foreground="#999"
                               VerticalAlignment="Center"
                               Margin="0,0,20,0" />
                </Grid>
            </Border>
        </Grid>
    </Border>

    <!-- Password Binding Helper -->
    <Window.Resources>
        <local:PasswordBoxAssistant x:Key="PasswordBoxAssistant" />
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0"
                                     To="1"
                                     Duration="0:0:0.3" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
